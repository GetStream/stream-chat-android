package io.getstream.chat.android.livedata.controller.helper

import io.getstream.chat.android.client.models.Message

class MessageHelper {

    fun updateValidAttachmentsUrl(oldMessages: Map<String, Message>, newMessages: List<Message>): List<Message> {
        return newMessages
    }
}