<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/stream_ui_gallery_default_bar_height"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:titleTextAppearance="@style/StreamUIAttachmentGalleryToolbarTitleTextAppearance"
        app:subtitleTextAppearance="@style/StreamUIAttachmentGalleryToolbarSubtitleTextAppearance"
        app:navigationIcon="@drawable/stream_ui_ic_close"
        app:buttonGravity="center_vertical"
        app:titleMarginBottom="8dp">

        <ImageView
            android:id="@+id/menuButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/stream_ui_ic_three_dots_menu"
            android:layout_marginEnd="@dimen/stream_ui_spacing_small"
            android:layout_gravity="end"/>

    </androidx.appcompat.widget.Toolbar>

    <io.getstream.chat.android.ui.images.AttachmentGallery
        android:id="@+id/attachmentGallery"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>